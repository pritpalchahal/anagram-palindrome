# anagram-palindrome

Method returns true if an input string is either a palindrome or an anagram of a palindrome, false otherwise.

Assumptions for input string:

•	There will be no whitespace in the string.
•	All characters in the string will be lower case.
•	The string will only contain alpha characters a-z.
